<Page
    x:Class="BDSA2017.Lecture09.MVVM.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BDSA2017.Lecture09.MVVM.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <SolidColorBrush x:Key="ButtonColor" Color="GreenYellow" />
        <SolidColorBrush x:Key="BackgroundColor" Color="BlueViolet" />
    </Page.Resources>
    <Grid Background="{StaticResource BackgroundColor}">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="20" Content="Albums" Command="{Binding GoToAlbumsPageCommand}" Width="200" Height="100" FontSize="30" Background="{StaticResource ButtonColor}" />
            <Button Visibility="Collapsed" Margin="20" Content="Contacts" Command="{Binding GoToContactsPageCommand}" Width="200" Height="100" FontSize="30" Background="{StaticResource ButtonColor}" />
        </StackPanel>
    </Grid>
</Page>
